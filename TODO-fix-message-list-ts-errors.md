# Fix TypeScript Errors in MessageList.vue

## Steps:
- [x] 1. Add const messageStore = useMessageStore() and const userStore = useUserStore() declarations
- [x] 2. Fix v-for typing and add :key="message.id"
- [x] 3. Verify no TS errors and runtime works
- [x] 4. Archive TODO
